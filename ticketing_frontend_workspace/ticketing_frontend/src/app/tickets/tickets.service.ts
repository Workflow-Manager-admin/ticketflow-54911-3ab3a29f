// PUBLIC_INTERFACE
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class TicketsService {
  /** This is a stub for ticket fetching, updating, etc. */
  constructor() { }
}
